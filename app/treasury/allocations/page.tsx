import IncomeAllocationsPage from '@/components/treasurydashboard/incomeAllocations/IncomeAllocationsPage';

/**
 * /treasury/allocations — Church vs conference income demarcation (splits from backend).
 */
export default function Page() {
  return <IncomeAllocationsPage />;
}
